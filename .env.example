# Required
TELEGRAM_BOT_TOKEN=""
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/birthday_reminder_bot?schema=public"
DKRON_API_URL="http://localhost:8080"
INTERNAL_WEBHOOK_SECRET="change-me-at-least-16-chars-long"

# Required (one of these must be set) — how dkron reaches the bot's /internal/fire-reminder
BOT_INTERNAL_URL="http://localhost:3000"
# RAILWAY_PRIVATE_DOMAIN=  # set automatically by Railway in production

# Optional (defaults shown)
# PORT=3000
# TELEGRAM_WEBHOOK_PATH=/telegram/webhook

# Optional — only needed when DATABASE_URL points at a pgBouncer/Neon pooler
# (Prisma migrations need a direct session-mode connection for advisory locks).
# DIRECT_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/birthday_reminder_bot?schema=public"
